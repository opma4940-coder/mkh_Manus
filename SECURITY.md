# 🔐 سياسة الأمان - mkh_Manus

<div dir="rtl">

## نظرة عامة

يأخذ نظام **mkh_Manus** الأمان على محمل الجد. تم تصميم النظام مع وضع أفضل ممارسات الأمان في الاعتبار لحماية بياناتك ومعلوماتك الحساسة.

---

## 🔑 إدارة الأسرار والمفاتيح

### تخزين المفاتيح

يستخدم النظام عدة طبقات لحماية المفاتيح والأسرار:

1. **تشفير المفاتيح في قاعدة البيانات**
   - جميع مفاتيح API يتم تشفيرها باستخدام Fernet (AES-128)
   - يتم تخزين المفاتيح المشفرة فقط في قاعدة البيانات
   - لا يتم تخزين المفاتيح الأصلية أبداً

2. **متغيرات البيئة**
   - يجب تخزين `FERNET_KEY` و `JWT_SECRET_KEY` في متغيرات البيئة
   - لا تقم أبداً بتضمين ملف `.env` في نظام التحكم بالإصدارات
   - استخدم `.env.example` كقالب فقط

3. **إدارة الأسرار في الإنتاج**
   - استخدم أدوات إدارة الأسرار مثل:
     - HashiCorp Vault
     - AWS Secrets Manager
     - Azure Key Vault
     - Kubernetes Secrets

### إنشاء مفاتيح آمنة

```bash
# إنشاء Fernet Key
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# إنشاء JWT Secret
openssl rand -hex 32
```

---

## 🛡️ المصادقة والتفويض

### نظام JWT

- يستخدم النظام JSON Web Tokens للمصادقة
- مدة صلاحية الـ Token: 24 ساعة (قابلة للتخصيص)
- يتم تجديد الـ Token تلقائياً عند اقتراب انتهاء صلاحيته

### كلمات المرور

- يتم تشفير كلمات المرور باستخدام bcrypt
- الحد الأدنى لطول كلمة المرور: 8 أحرف
- يُنصح باستخدام كلمات مرور قوية تحتوي على:
  - أحرف كبيرة وصغيرة
  - أرقام
  - رموز خاصة

---

## 📁 أمان الملفات

### فحص الملفات المرفوعة

يقوم النظام بفحص جميع الملفات المرفوعة:

1. **التحقق من نوع الملف**
   - فحص امتداد الملف
   - التحقق من MIME type

2. **فحص الحجم**
   - الحد الأقصى لحجم الملف: 100 MB (قابل للتخصيص)

3. **فحص المحتوى**
   - فحص الملفات ضد التهديدات المعروفة
   - استخدام ClamAV للفحص الأمني (اختياري)

### الروابط الموقعة (Presigned URLs)

- جميع روابط الرفع والتحميل موقعة ومؤقتة
- مدة صلاحية روابط الرفع: 15 دقيقة
- مدة صلاحية روابط التحميل: 1 ساعة

---

## 🔒 قيود الصلاحيات

### صلاحيات المستخدمين

| الدور | الصلاحيات |
|:---|:---|
| **Admin** | الوصول الكامل لجميع الميزات والإعدادات |
| **User** | إنشاء وإدارة المهام الخاصة فقط |
| **Guest** | القراءة فقط (إن تم تفعيله) |

### صلاحيات قاعدة البيانات

- استخدم مستخدم قاعدة بيانات منفصل لكل بيئة
- لا تستخدم مستخدم `postgres` الرئيسي في الإنتاج
- قم بتقييد الصلاحيات حسب الحاجة فقط

---

## 🔍 فحص التبعيات

### الفحص التلقائي

يتم فحص التبعيات تلقائياً في CI/CD pipeline:

```bash
# فحص تبعيات Python
pip install safety
safety check --file requirements.txt

# فحص تبعيات Node.js
pnpm audit

# فحص الأسرار في الكود
pip install detect-secrets
detect-secrets scan
```

### تحديث التبعيات

- قم بتحديث التبعيات بانتظام
- راجع التحديثات الأمنية أولاً بأول
- استخدم أدوات مثل Dependabot

---

## 🌐 أمان الشبكة

### HTTPS

- **يجب** استخدام HTTPS في بيئة الإنتاج
- استخدم شهادات SSL/TLS صالحة
- قم بتفعيل HSTS (HTTP Strict Transport Security)

### CORS

- قم بتكوين CORS بشكل صحيح
- لا تستخدم `*` في بيئة الإنتاج
- حدد النطاقات المسموح بها بدقة

### Rate Limiting

- تم تفعيل Rate Limiting على جميع endpoints
- الحد الأقصى: 100 طلب/دقيقة لكل IP
- يمكن تخصيص الحدود حسب الحاجة

---

## 🐳 أمان Docker

### أفضل الممارسات

1. **لا تستخدم root**
   - جميع الحاويات تعمل بمستخدم غير root

2. **تحديث الصور**
   - استخدم أحدث إصدارات الصور الأساسية
   - قم بفحص الصور بانتظام

3. **فحص الثغرات**
   ```bash
   # فحص صورة Docker
   docker scan mkh_manus:latest
   
   # استخدام Trivy
   trivy image mkh_manus:latest
   ```

---

## 📊 التسجيل والمراقبة

### السجلات

- يتم تسجيل جميع العمليات الحساسة
- لا يتم تسجيل كلمات المرور أو المفاتيح
- يتم تخزين السجلات بشكل آمن

### المراقبة

- مراقبة محاولات الوصول الفاشلة
- تنبيهات للأنشطة المشبوهة
- مراقبة استخدام الموارد

---

## 🚨 الإبلاغ عن الثغرات

إذا اكتشفت ثغرة أمنية في النظام:

1. **لا تنشر** الثغرة علناً
2. أرسل تقريراً مفصلاً إلى: security@mkh-manus.local
3. قم بتضمين:
   - وصف الثغرة
   - خطوات إعادة الإنتاج
   - التأثير المحتمل
   - اقتراحات للإصلاح (إن وجدت)

سنقوم بالرد خلال 48 ساعة وإصدار تحديث أمني في أقرب وقت ممكن.

---

## ✅ قائمة التحقق الأمنية

قبل النشر في الإنتاج، تأكد من:

- [ ] تغيير جميع كلمات المرور الافتراضية
- [ ] إنشاء مفاتيح تشفير قوية جديدة
- [ ] تفعيل HTTPS
- [ ] تكوين CORS بشكل صحيح
- [ ] تفعيل Rate Limiting
- [ ] إعداد النسخ الاحتياطي التلقائي
- [ ] تكوين المراقبة والتنبيهات
- [ ] مراجعة صلاحيات قاعدة البيانات
- [ ] فحص جميع التبعيات
- [ ] إزالة ملف `.env` من Git
- [ ] تفعيل الفحص الأمني للملفات
- [ ] مراجعة سجلات الوصول

---

## 📚 مراجع إضافية

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CWE Top 25](https://cwe.mitre.org/top25/)
- [Docker Security Best Practices](https://docs.docker.com/engine/security/)
- [FastAPI Security](https://fastapi.tiangolo.com/tutorial/security/)

</div>
