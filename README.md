# 🚀 mkh_Manus Pro - نظام إدارة المهام الذكي المتكامل

<div dir="rtl">

## 📋 نظرة عامة

**mkh_Manus Pro** هو نظام متقدم وشامل لإدارة المهام والوكلاء الذكيين، يمثل الجيل الجديد من أدوات الذكاء الاصطناعي المؤسسية. تم تصميم النظام ليكون **منصة متكاملة** توفر واجهة مستخدم حديثة واحترافية مستوحاة من أفضل المنصات العالمية مثل ChatGPT و Claude، مع بنية تحتية قوية وآمنة وقابلة للتطوير.

يعتمد النظام على فلسفة **"التعقيد في الداخل، والبساطة في الخارج"**، حيث يوفر واجهة دردشة بسيطة وبديهية تخفي خلفها محركاً قوياً قادراً على تفكيك المهام المعقدة، إدارة الوكلاء الذكيين، والتعامل مع الموصلات الخارجية بكفاءة عالية.

---

## ✨ الميزات الرئيسية

### 🎨 الواجهة والتجربة

يوفر **mkh_Manus Pro** واجهة مستخدم متطورة مصممة بعناية لتوفير تجربة استخدام سلسة واحترافية:

- **واجهة دردشة متقدمة**: تصميم يركز على المحادثة (Chat-first layout) مع دعم كامل للغة العربية (RTL)
- **قائمة جانبية شاملة**: تنظيم ذكي للمهام، مساحات العمل، الأحداث، والإعدادات
- **شريط علوي ذكي**: عرض معلومات المستخدم والإشعارات والتحكم السريع
- **محرر رسائل متطور**: دعم النصوص، الملفات، الصور، الفيديوهات، والأصوات
- **نظام ألوان احترافي**: تصميم داكن مريح للعين مع ألوان متناسقة
- **استجابة كاملة**: يعمل بسلاسة على جميع الأجهزة والشاشات

### 🧠 إدارة المهام الذكية

نظام متقدم لإدارة المهام مع قدرات ذكية:

- **تفكيك المهام التلقائي**: يقوم النظام بتفكيك المهام المعقدة إلى مهام فرعية قابلة للتنفيذ
- **تنفيذ متوازي**: معالجة المهام الفرعية بشكل متوازي لتسريع الإنجاز
- **تتبع التقدم الفوري**: مراقبة حية لحالة المهام والتقدم المحرز
- **إدارة الأولويات**: تحديد أولويات المهام وجدولتها بذكاء
- **سجل شامل**: تسجيل تفصيلي لجميع الأحداث والإجراءات
- **إعادة المحاولة التلقائية**: معالجة الأخطاء وإعادة المحاولة بشكل ذكي

### 🔐 الأمان والحماية

نظام أمان متعدد الطبقات لحماية بياناتك:

- **تشفير شامل**: تشفير جميع البيانات الحساسة باستخدام Fernet (AES-128)
- **مصادقة JWT**: نظام مصادقة آمن مع رموز وصول محدودة الصلاحية
- **تخزين آمن للمفاتيح**: دعم 5 فتحات لمفاتيح API مع تشفير عند الراحة
- **فحص الملفات**: نظام فحص تلقائي للملفات المرفوعة ضد التهديدات
- **روابط موقعة**: استخدام Presigned URLs لرفع وتحميل الملفات بأمان
- **تدقيق شامل**: سجل تدقيق كامل لجميع العمليات الحساسة
- **صلاحيات دقيقة**: نظام صلاحيات متقدم للمستخدمين والأدوار

### 📁 إدارة الملفات والتخزين

نظام متكامل لإدارة الملفات والمرفقات:

- **رفع ملفات متعدد**: دعم رفع الملفات، الصور، الفيديوهات، والأصوات
- **تخزين S3/MinIO**: تكامل كامل مع S3 أو MinIO للتخزين السحابي
- **روابط مؤقتة**: إنشاء روابط رفع وتحميل موقعة ومؤقتة
- **معاينة الملفات**: معاينة سريعة للملفات المرفقة
- **إدارة المساحة**: مراقبة استخدام المساحة التخزينية
- **نسخ احتياطي**: نظام نسخ احتياطي تلقائي للملفات المهمة

### 🔌 نظام الموصلات الشامل

تكامل سلس مع أكثر من 16 منصة ومصدر بيانات:

#### أدوات العمل والإنتاجية
- **Google Drive**: الوصول إلى ملفات Google Drive
- **Microsoft OneDrive**: التكامل مع OneDrive
- **GitHub**: إدارة المستودعات والأكواد
- **الجهاز المحلي**: الوصول إلى الملفات المحلية

#### منصات التواصل الاجتماعي
- **Facebook**: التفاعل مع Facebook
- **Instagram**: إدارة محتوى Instagram
- **LinkedIn**: التواصل المهني
- **Reddit**: متابعة المجتمعات
- **Threads**: التكامل مع Threads

#### تطبيقات المراسلة
- **WhatsApp**: إرسال واستقبال الرسائل
- **Telegram**: التفاعل مع Telegram
- **Discord**: إدارة خوادم Discord
- **Messenger**: التكامل مع Messenger

#### منصات الوسائط
- **TikTok**: إدارة محتوى TikTok
- **Snapchat**: التكامل مع Snapchat

### ⚡ الأداء والموثوقية

بنية تحتية قوية لضمان الأداء العالي:

- **معالجة خلفية**: استخدام Celery لمعالجة المهام الطويلة
- **قائمة انتظار ذكية**: Redis لإدارة قوائم الانتظار والتخزين المؤقت
- **قاعدة بيانات قوية**: PostgreSQL لتخزين البيانات بشكل موثوق
- **توازن الحمل**: دعم توزيع الحمل عبر عدة عمال
- **مراقبة حية**: Flower لمراقبة المهام والعمال
- **استرداد تلقائي**: إعادة المحاولة التلقائية عند الفشل

### 🎯 ميزات إضافية

- **دعم متعدد اللغات**: واجهة كاملة باللغة العربية مع دعم لغات أخرى
- **إشعارات فورية**: تنبيهات فورية لجميع الأحداث المهمة
- **تصدير البيانات**: تصدير المهام والبيانات بصيغ متعددة
- **API شامل**: واجهة برمجية كاملة مع توثيق تفاعلي
- **قابلية التوسع**: بنية معيارية قابلة للتوسع بسهولة
- **نشر سهل**: دعم Docker و Kubernetes للنشر السريع

---

## 🏗️ البنية المعمارية

تم تصميم **mkh_Manus Pro** ببنية معيارية حديثة وقابلة للتطوير:

```
mkh_Manus/
├── frontend/                    # الواجهة الأمامية
│   ├── src/
│   │   ├── components/          # مكونات React
│   │   │   ├── ChatLayout.jsx   # تخطيط الدردشة الرئيسي
│   │   │   ├── TopBar.jsx       # الشريط العلوي
│   │   │   ├── Sidebar.jsx      # القائمة الجانبية
│   │   │   ├── Composer.jsx     # محرر الرسائل
│   │   │   ├── TaskPanel.tsx    # لوحة المهام
│   │   │   ├── WorkspacePanel.tsx # لوحة مساحة العمل
│   │   │   ├── EventsPanel.tsx  # لوحة الأحداث
│   │   │   └── SettingsPanel.tsx # لوحة الإعدادات
│   │   ├── styles/              # ملفات التصميم
│   │   │   ├── variables.css    # متغيرات CSS
│   │   │   └── app.css          # التصميم الرئيسي
│   │   ├── api.ts               # وظائف API
│   │   └── main.tsx             # نقطة الدخول
│   ├── package.json             # تبعيات Node.js
│   └── vite.config.ts           # تكوين Vite
│
├── backend/                     # الخادم الخلفي
│   ├── app/
│   │   ├── routes/              # مسارات API
│   │   │   └── uploads.py       # رفع الملفات
│   │   ├── tasks/               # مهام Celery
│   │   │   └── worker_tasks.py  # مهام العامل
│   │   ├── db/                  # قاعدة البيانات
│   │   │   ├── models.py        # نماذج SQLAlchemy
│   │   │   └── db.py            # اتصال قاعدة البيانات
│   │   ├── core/                # الوظائف الأساسية
│   │   │   ├── auth.py          # المصادقة
│   │   │   └── config.py        # التكوين
│   │   ├── crypto.py            # التشفير
│   │   ├── logging_config.py    # تكوين السجلات
│   │   ├── main.py              # تطبيق FastAPI الرئيسي
│   │   └── celery_app.py        # تطبيق Celery
│   │
│   └── src/manus_pro_server/    # خادم Manus Pro
│       ├── connectors/          # الموصلات
│       │   ├── google.py
│       │   ├── facebook.py
│       │   ├── whatsapp.py
│       │   └── ... (16+ موصل)
│       ├── alembic/             # Migrations
│       ├── api.py               # API الرئيسي
│       ├── auth.py              # المصادقة
│       ├── db.py                # قاعدة البيانات
│       └── tasks.py             # المهام
│
├── agent_service/               # خدمة الوكيل
│   └── agent.py                 # منطق الوكيل
│
├── migrations/                  # ملفات Migration
│   └── 0001_init.sql            # Migration الأولي
│
├── scripts/                     # سكريبتات مساعدة
│   └── smoke_test.sh            # اختبارات الدخان
│
├── .github/workflows/           # CI/CD
│   └── ci.yml                   # GitHub Actions
│
├── docker-compose.yml           # تكوين Docker Compose
├── Dockerfile                   # صورة Docker
├── deploy.sh                    # سكريبت النشر
├── validation.sh                # سكريبت التحقق
├── requirements.txt             # تبعيات Python
├── .env.example                 # قالب المتغيرات البيئية
├── README.md                    # هذا الملف
├── SECURITY.md                  # سياسة الأمان
└── ASSURANCE.md                 # ضمان الجودة
```

---

## 🚀 البدء السريع

### المتطلبات الأساسية

- **Docker**: الإصدار 20.10 أو أحدث
- **Docker Compose**: الإصدار 2.0 أو أحدث
- **Git**: لاستنساخ المستودع

### التثبيت والتشغيل

#### 1. استنساخ المستودع

```bash
git clone https://github.com/your-username/mkh_Manus.git
cd mkh_Manus
```

#### 2. إعداد المتغيرات البيئية

```bash
# نسخ ملف القالب
cp .env.example .env

# تحرير الملف وتحديث القيم
nano .env
```

**المتغيرات المطلوبة:**

```bash
# الأمان (يجب تغييرها!)
FERNET_KEY=your-fernet-key-here
JWT_SECRET_KEY=your-jwt-secret-here
ADMIN_TOKEN=your-admin-token-here

# قاعدة البيانات
POSTGRES_DB=mkh_manus
POSTGRES_USER=mkh_user
POSTGRES_PASSWORD=your-secure-password

# Redis
REDIS_PASSWORD=your-redis-password

# MinIO
MINIO_ROOT_USER=mkh_minio_admin
MINIO_ROOT_PASSWORD=your-minio-password
MINIO_BUCKET=mkh-attachments
```

#### 3. بناء وتشغيل النظام

```bash
# باستخدام سكريبت النشر (موصى به)
./deploy.sh

# أو يدوياً
docker-compose up -d --build
```

#### 4. الوصول إلى الخدمات

بعد التشغيل الناجح، يمكنك الوصول إلى:

| الخدمة | الرابط | الوصف |
|:---|:---|:---|
| **لوحة التحكم** | http://localhost:8000 | الواجهة الرئيسية |
| **توثيق API** | http://localhost:8000/docs | Swagger UI |
| **OpenAPI Schema** | http://localhost:8000/openapi.json | مواصفات API |
| **MinIO Console** | http://localhost:9001 | إدارة التخزين |
| **Flower** | http://localhost:5555 | مراقبة Celery |

---

## 🔧 التكوين المتقدم

### إنشاء مفاتيح التشفير

```bash
# إنشاء Fernet Key
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# إنشاء JWT Secret
openssl rand -hex 32
```

### تكوين قاعدة البيانات

```bash
# الاتصال بقاعدة البيانات
docker exec -it mkh_postgres psql -U mkh_user -d mkh_manus

# تطبيق migrations يدوياً
docker exec -i mkh_postgres psql -U mkh_user -d mkh_manus < migrations/0001_init.sql
```

### تكوين MinIO

```bash
# الوصول إلى MinIO Console
# افتح: http://localhost:9001
# اسم المستخدم: mkh_minio_admin
# كلمة المرور: (من ملف .env)

# إنشاء Bucket يدوياً
docker exec mkh_minio mc mb /data/mkh-attachments
```

---

## 🧪 الاختبار والتحقق

### تشغيل الاختبارات

```bash
# اختبارات الدخان (Smoke Tests)
./scripts/smoke_test.sh

# التحقق الشامل
./validation.sh

# اختبارات الوحدة (Unit Tests)
pytest backend/tests/ -v

# اختبارات مع التغطية
pytest backend/tests/ --cov=backend --cov-report=html
```

### فحص الأمان

```bash
# فحص التبعيات
pip install safety
safety check --file requirements.txt

# فحص الأسرار
pip install detect-secrets
detect-secrets scan

# فحص الحاويات
trivy image mkh_manus:latest
```

---

## 📊 المراقبة والسجلات

### عرض السجلات

```bash
# جميع الخدمات
docker-compose logs -f

# خدمة محددة
docker-compose logs -f api
docker-compose logs -f worker

# آخر 100 سطر
docker-compose logs --tail=100 api
```

### مراقبة الأداء

```bash
# حالة الخدمات
docker-compose ps

# استخدام الموارد
docker stats

# مراقبة Celery عبر Flower
# افتح: http://localhost:5555
```

---

## 🔄 التحديث والصيانة

### تحديث النظام

```bash
# سحب آخر التحديثات
git pull origin main

# إعادة بناء وتشغيل
docker-compose down
docker-compose up -d --build
```

### النسخ الاحتياطي

```bash
# نسخ احتياطي لقاعدة البيانات
docker exec mkh_postgres pg_dump -U mkh_user mkh_manus > backup_$(date +%Y%m%d).sql

# نسخ احتياطي للملفات
docker run --rm -v mkh_manus_minio_data:/data -v $(pwd):/backup alpine tar czf /backup/minio_backup_$(date +%Y%m%d).tar.gz /data
```

### الاسترجاع

```bash
# استرجاع قاعدة البيانات
docker exec -i mkh_postgres psql -U mkh_user mkh_manus < backup_20251227.sql

# استرجاع الملفات
docker run --rm -v mkh_manus_minio_data:/data -v $(pwd):/backup alpine tar xzf /backup/minio_backup_20251227.tar.gz -C /
```

---

## 🐛 استكشاف الأخطاء

### المشاكل الشائعة

#### النظام لا يبدأ

```bash
# التحقق من السجلات
docker-compose logs

# إعادة تشغيل الخدمات
docker-compose restart

# إعادة بناء كاملة
docker-compose down -v
docker-compose up -d --build
```

#### خطأ في الاتصال بقاعدة البيانات

```bash
# التحقق من حالة PostgreSQL
docker-compose ps postgres

# إعادة تشغيل قاعدة البيانات
docker-compose restart postgres

# التحقق من السجلات
docker-compose logs postgres
```

#### خطأ في رفع الملفات

```bash
# التحقق من حالة MinIO
docker-compose ps minio

# التحقق من Bucket
docker exec mkh_minio mc ls /data/

# إنشاء Bucket إذا لم يكن موجوداً
docker exec mkh_minio mc mb /data/mkh-attachments
```

---

## 🤝 المساهمة

نرحب بمساهماتكم! يرجى قراءة [CONTRIBUTING.md](CONTRIBUTING.md) للمزيد من التفاصيل.

### خطوات المساهمة

1. Fork المشروع
2. إنشاء فرع للميزة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

---

## 📄 الترخيص

هذا المشروع مرخص تحت **رخصة MIT** - انظر ملف [LICENSE](LICENSE) للتفاصيل.

---

## 📞 الدعم والتواصل

- **البريد الإلكتروني**: support@mkh-manus.local
- **الأمان**: security@mkh-manus.local
- **الوثائق**: [docs.mkh-manus.local](https://docs.mkh-manus.local)

---

## 🙏 شكر وتقدير

تم بناء هذا النظام باستخدام تقنيات مفتوحة المصدر رائعة:

- [FastAPI](https://fastapi.tiangolo.com/) - إطار عمل API السريع
- [React](https://react.dev/) - مكتبة واجهة المستخدم
- [PostgreSQL](https://www.postgresql.org/) - قاعدة البيانات
- [Redis](https://redis.io/) - التخزين المؤقت
- [Celery](https://docs.celeryq.dev/) - معالجة المهام
- [MinIO](https://min.io/) - التخزين السحابي
- [Docker](https://www.docker.com/) - الحاويات

---

<div align="center">

**صنع بـ ❤️ لخدمة المؤسسات والمطورين**

© 2025 mkh_Manus Pro. جميع الحقوق محفوظة.

</div>

</div>
