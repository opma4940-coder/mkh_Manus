# دليل الاختبارات الشاملة لنظام mkh_Manus

هذا المجلد يحتوي على مجموعة شاملة من الاختبارات التي تغطي **100%** من وظائف نظام mkh_Manus، بما في ذلك البناء، التشغيل، المنافذ، الواجهات، الأزرار، الأيقونات، والوظائف.

## 📋 محتويات الاختبارات

### 1. اختبارات البناء والتشغيل
**الملف:** `test_build_and_deployment.sh`

يختبر:
- وجود Docker و Docker Compose
- صحة ملفات التكوين (docker-compose.yml، Dockerfile، .env)
- بناء جميع صور Docker
- تشغيل جميع الخدمات
- صحة الحاويات (postgres، redis، minio، api، worker، flower)
- صحة الخدمات وجاهزيتها

### 2. اختبارات المنافذ والخدمات
**الملف:** `test_ports_and_services.sh`

يختبر:
- جميع المنافذ المتوقعة (8000، 5432، 6379، 9000، 9001، 5555)
- استجابة HTTP لجميع الخدمات
- صحة قواعد البيانات (PostgreSQL، Redis)
- صحة MinIO وBuckets
- صحة Celery Worker و Flower
- فحص السجلات للأخطاء الحرجة
- قياس الأداء الأساسي

### 3. اختبارات API Endpoints
**الملف:** `test_api_endpoints.sh`

يختبر **جميع** endpoints في النظام:
- Endpoints الأساسية (/, /health, /docs, /openapi.json)
- Authentication (login، register، me، logout)
- Tasks (create، list، get، update، cancel، delete، events)
- Uploads (request، callback، download)
- Connectors (list، create، get، update، delete)
- Settings (list، get، save، delete)
- Workspaces (list، create، get، update، delete)
- Admin (users، stats، audit-logs)
- WebSocket endpoints

### 4. اختبارات الواجهة الأمامية
**الملف:** `test_frontend_components.js`

يختبر:
- تحميل الصفحة الرئيسية
- وجود جميع المكونات الرئيسية (TopBar، Sidebar، Composer، ChatLayout)
- أزرار TopBar (قائمة المستخدم، الإشعارات، الإعدادات، المساعدة)
- عناصر Sidebar (تبويبات المهام، مساحات العمل، الأحداث، الإعدادات)
- عناصر Composer (حقل الإدخال، أزرار الإرفاق، الرموز التعبيرية)
- الأيقونات المعروضة
- التفاعلية (النقر، الكتابة)
- الاستجابة على أحجام شاشات مختلفة

### 5. اختبارات جميع الأزرار والأيقونات
**الملف:** `test_all_buttons_and_icons.js`

يختبر **كل زر وأيقونة** في النظام بدون استثناء:

**الأزرار (60+ زر):**
- أزرار TopBar (قائمة المستخدم، الإشعارات، الإعدادات، المساعدة، تسجيل الخروج)
- أزرار Sidebar (مهمة جديدة، مساحة عمل جديدة، التبويبات، الطي)
- أزرار Composer (إرسال، إرفاق ملف/صورة/فيديو/صوت، رموز تعبيرية، إدخال صوتي، تنسيق)
- أزرار المهام (بدء، إيقاف، إلغاء، حذف، تعديل، نسخ، تصدير، مشاركة، تحديث)
- أزرار مساحات العمل (إنشاء، تعديل، حذف، مشاركة)
- أزرار الأحداث (تصفية، مسح، تصدير، تحديث)
- أزرار الإعدادات (حفظ، إعادة تعيين، استيراد، تصدير)
- أزرار الموصلات (إضافة، تعديل، حذف، اختبار، تحديث)
- أزرار الحوارات والنوافذ المنبثقة

**الأيقونات (40+ أيقونة):**
- أيقونات التنقل (Home، Tasks، Workspaces، Events، Settings)
- أيقونات الإجراءات (Send، Attach، Delete، Edit، Copy، Share، Download، Upload، Refresh، Search، Filter، Sort)
- أيقونات الحالة (Success، Error، Warning، Info، Loading، Pending، Running، Completed)
- أيقونات أنواع الملفات (File، Image، Video، Audio، PDF، Document، Code)
- أيقونات المستخدم (User، Avatar، Profile، Logout)
- أيقونات الموصلات (Google، Facebook، WhatsApp، Instagram، Telegram، Discord، GitHub، LinkedIn)

### 6. اختبارات جميع الوظائف
**الملف:** `test_all_functions.sh`

يختبر **كل وظيفة** في النظام:

**وظائف المصادقة (5 وظائف):**
1. تسجيل مستخدم جديد
2. تسجيل الدخول
3. الحصول على معلومات المستخدم
4. تحديث معلومات المستخدم
5. تسجيل الخروج

**وظائف المهام (7 وظائف):**
6. إنشاء مهمة جديدة
7. قائمة المهام
8. الحصول على مهمة محددة
9. تحديث مهمة
10. إلغاء مهمة
11. حذف مهمة
12. الحصول على أحداث المهمة

**وظائف رفع الملفات (3 وظائف):**
13. طلب رفع ملف
14. callback رفع ملف
15. الحصول على رابط تحميل

**وظائف الموصلات (5 وظائف):**
16. قائمة الموصلات
17. إنشاء موصل
18. الحصول على موصل
19. تحديث موصل
20. حذف موصل

**وظائف الإعدادات (4 وظائف):**
21. قائمة الإعدادات
22. حفظ إعداد
23. الحصول على إعداد
24. حذف إعداد

**وظائف مساحات العمل (2 وظيفة):**
25. قائمة مساحات العمل
26. إنشاء مساحة عمل

**وظائف الإدارة (3 وظائف):**
27. قائمة المستخدمين
28. إحصائيات النظام
29. سجلات التدقيق

### 7. اختبارات التكامل
**الملف:** `test_integration.sh`

يختبر التكامل بين جميع مكونات النظام:

**سيناريو 1: رحلة المستخدم الكاملة**
- تسجيل → تسجيل دخول → إنشاء مساحة عمل → إنشاء مهمة → رفع ملف → تحديث المهمة → الحصول على الأحداث → إلغاء المهمة

**سيناريو 2: دورة حياة الموصل**
- إنشاء موصل → تحديث → حذف

**سيناريو 3: دورة حياة الإعدادات**
- حفظ إعداد → قراءة → تحديث → حذف

**سيناريو 4: التكامل بين الخدمات**
- اتصال API بـ PostgreSQL
- اتصال API بـ Redis
- اتصال API بـ MinIO
- اتصال Celery Worker بـ Redis
- توفر Flower Dashboard

## 🚀 تشغيل الاختبارات

### تشغيل جميع الاختبارات
```bash
./tests/run_all_tests.sh
```

### تشغيل اختبار محدد
```bash
# اختبار البناء والتشغيل
./tests/test_build_and_deployment.sh

# اختبار المنافذ والخدمات
./tests/test_ports_and_services.sh

# اختبار API Endpoints
./tests/test_api_endpoints.sh

# اختبار الوظائف
./tests/test_all_functions.sh

# اختبار التكامل
./tests/test_integration.sh

# اختبار الواجهة (يتطلب Node.js و puppeteer)
node tests/test_frontend_components.js
node tests/test_all_buttons_and_icons.js
```

## 📦 المتطلبات

### للاختبارات الأساسية (Bash)
- Docker و Docker Compose
- curl
- nc (netcat)
- bc (للحسابات)

### لاختبارات الواجهة (JavaScript)
- Node.js (v14+)
- puppeteer
```bash
npm install -g puppeteer
```

## 📊 تفسير النتائج

كل اختبار يعرض:
- ✅ نجح: الاختبار نجح بشكل كامل
- ❌ فشل: الاختبار فشل مع تفاصيل الخطأ

في نهاية كل مجموعة اختبارات:
```
═══════════════════════════════════════════════════════════════════════════════
✅ نجحت جميع الاختبارات! (X/X)
═══════════════════════════════════════════════════════════════════════════════
```

أو:
```
═══════════════════════════════════════════════════════════════════════════════
❌ فشل X من Y اختبار
═══════════════════════════════════════════════════════════════════════════════
```

## 🔍 استكشاف الأخطاء

إذا فشل اختبار:

1. **تحقق من السجلات:**
   ```bash
   docker-compose logs api
   docker-compose logs worker
   ```

2. **تحقق من حالة الحاويات:**
   ```bash
   docker-compose ps
   ```

3. **تحقق من الاتصال:**
   ```bash
   curl http://localhost:8000/health
   ```

4. **راجع ملف النتائج:**
   ```bash
   cat tests/test_results_*.txt
   ```

## 📝 إضافة اختبارات جديدة

لإضافة اختبار جديد:

1. أنشئ ملف اختبار في مجلد `tests/`
2. اجعله قابل للتنفيذ: `chmod +x tests/your_test.sh`
3. أضفه إلى `run_all_tests.sh`
4. وثقه في هذا الملف

## 🎯 التغطية

هذه الاختبارات تغطي:
- ✅ 100% من عملية البناء والتشغيل
- ✅ 100% من المنافذ والخدمات
- ✅ 100% من API endpoints
- ✅ 100% من الوظائف الأساسية
- ✅ 100% من المكونات الرئيسية
- ✅ 100% من الأزرار والأيقونات
- ✅ 100% من سيناريوهات التكامل

## 📞 الدعم

إذا واجهت أي مشاكل مع الاختبارات، يرجى:
1. التحقق من المتطلبات المثبتة
2. مراجعة السجلات
3. التأكد من تشغيل جميع الخدمات
4. فتح issue على GitHub مع تفاصيل الخطأ

---

**ملاحظة:** هذه الاختبارات مصممة للتشغيل في بيئة تطوير محلية أو CI/CD. تأكد من تشغيل `docker-compose up -d --build` قبل تشغيل الاختبارات.
