# 🤝 دليل المساهمة - mkh_Manus

<div dir="rtl">

شكراً لاهتمامك بالمساهمة في مشروع **mkh_Manus**! هذا الدليل سيساعدك على فهم كيفية المساهمة بشكل فعال.

## 📋 جدول المحتويات

1. [قواعد السلوك](#قواعس-السلوك)
2. [كيفية المساهمة](#كيفية-المساهمة)
3. [عملية التطوير](#عملية-التطوير)
4. [معايير الكود](#معايير-الكود)
5. [الاختبارات](#الاختبارات)
6. [الالتزامات](#الالتزامات)
7. [Pull Requests](#pull-requests)

---

## 💬 قواعس السلوك

### التزامنا

نحن ملتزمون بتوفير بيئة ترحيبية وآمنة لجميع المساهمين بغض النظر عن:
- العمر والجنس والهوية الجنسية
- الإعاقة والمظهر الجسدي
- الخبرة والمستوى التعليمي
- الخلفية الاجتماعية والاقتصادية
- الأصل والعرق والدين

### السلوك المتوقع

- استخدام لغة احترامية وشاملة
- احترام وجهات النظر والتجارب المختلفة
- قبول النقد البناء بصدر رحب
- التركيز على ما هو أفضل للمجتمع
- إظهار التعاطف تجاه أعضاء المجتمع الآخرين

### السلوك غير المقبول

- استخدام لغة أو صور جنسية
- التنمر والمضايقة
- الهجمات الشخصية أو السياسية
- النشر الخاص للآخرين بدون موافقة
- أي سلوك يعتبر غير مناسب في بيئة مهنية

---

## 🚀 كيفية المساهمة

### الإبلاغ عن الأخطاء

إذا وجدت خطأ، يرجى فتح issue مع:

1. **عنوان واضح ووصفي**
2. **وصف مفصل للمشكلة**
3. **خطوات إعادة الإنتاج**
4. **السلوك المتوقع والفعلي**
5. **لقطات شاشة أو سجلات أخطاء**
6. **معلومات البيئة** (نظام التشغيل، الإصدار، إلخ)

### اقتراح ميزات جديدة

إذا كان لديك فكرة لميزة جديدة:

1. **فتح issue** مع عنوان يبدأ بـ `[Feature Request]`
2. **وصف الميزة** بالتفصيل
3. **شرح الفائدة** والحالات الاستخدام
4. **اقتراح التنفيذ** (اختياري)
5. **الاستماع للملاحظات** من المجتمع

### تحسينات التوثيق

التوثيق الجيد ضروري جداً! يمكنك:

1. إصلاح الأخطاء الإملائية
2. تحسين الوضوح والقراءة
3. إضافة أمثلة جديدة
4. ترجمة الوثائق
5. تحديث الروابط المكسورة

---

## 💻 عملية التطوير

### إعداد بيئة التطوير

```bash
# 1. Fork المستودع على GitHub

# 2. استنساخ النسخة الخاصة بك
git clone https://github.com/your-username/mkh_Manus.git
cd mkh_Manus

# 3. إضافة المستودع الأصلي كـ upstream
git remote add upstream https://github.com/original-owner/mkh_Manus.git

# 4. تثبيت التبعيات
pip install -r requirements.txt
pip install pytest pytest-cov black ruff

# 5. إنشاء فرع جديد
git checkout -b feature/your-feature-name
```

### الفروع

نستخدم الفروع التالية:

- **main**: الإصدار المستقر الحالي
- **develop**: فرع التطوير الرئيسي
- **feature/\***: فروع الميزات الجديدة
- **bugfix/\***: فروع إصلاح الأخطاء
- **docs/\***: فروع تحديثات التوثيق

### سير العمل

```bash
# 1. تحديث الفرع المحلي
git fetch upstream
git rebase upstream/develop

# 2. إجراء التعديلات
# ... قم بتعديل الملفات ...

# 3. تشغيل الاختبارات
pytest
black .
ruff check .

# 4. Commit التعديلات
git add .
git commit -m "feat: add amazing feature"

# 5. Push إلى النسخة الخاصة بك
git push origin feature/your-feature-name

# 6. فتح Pull Request على GitHub
```

---

## 📝 معايير الكود

### Python

```python
# استخدم type hints
def create_task(goal: str, budget: int) -> Task:
    """إنشاء مهمة جديدة.
    
    Args:
        goal: وصف المهمة
        budget: ميزانية التوكنات
        
    Returns:
        Task: المهمة المُنشأة
    """
    pass

# استخدم docstrings
class TaskManager:
    """مدير المهام.
    
    يدير إنشاء وتتبع المهام.
    """
    pass

# اتبع PEP 8
# - 4 مسافات للمحاذاة
# - سطور بحد أقصى 88 حرف
# - أسماء واضحة وموصوفة
```

### TypeScript/React

```typescript
// استخدم interfaces
interface Task {
  id: string;
  goal: string;
  status: 'pending' | 'running' | 'completed' | 'error';
  progress: number;
}

// استخدم functional components
const TaskPanel: React.FC<{ taskId: string }> = ({ taskId }) => {
  return <div>Task: {taskId}</div>;
};

// استخدم hooks
const [tasks, setTasks] = useState<Task[]>([]);
```

### CSS

```css
/* استخدم متغيرات CSS */
:root {
  --primary-color: #7c3aed;
  --text-color: #e2e8f0;
}

/* استخدم BEM naming */
.button {
  /* ... */
}

.button--primary {
  /* ... */
}

.button__icon {
  /* ... */
}

/* استخدم flexbox و grid */
.container {
  display: flex;
  gap: 1rem;
}
```

### التعليقات

```python
# ✅ تعليقات جيدة
# حساب المتوسط المتحرك
moving_average = sum(values[-window:]) / window

# ❌ تعليقات سيئة
# إضافة 1 إلى x
x = x + 1
```

---

## 🧪 الاختبارات

### كتابة الاختبارات

```python
import pytest
from manus_pro_server.tasks import create_task

def test_create_task():
    """اختبار إنشاء مهمة جديدة."""
    task = create_task("اختبار", 1000000)
    assert task.goal == "اختبار"
    assert task.status == "pending"

def test_create_task_with_invalid_budget():
    """اختبار إنشاء مهمة برقم ميزانية غير صحيح."""
    with pytest.raises(ValueError):
        create_task("اختبار", -1000)
```

### تشغيل الاختبارات

```bash
# تشغيل جميع الاختبارات
pytest

# تشغيل ملف معين
pytest tests/test_tasks.py

# تشغيل اختبار معين
pytest tests/test_tasks.py::test_create_task

# مع التغطية
pytest --cov=manus_pro_server

# مع التفاصيل
pytest -v

# مع الإيقاف عند أول فشل
pytest -x
```

### متطلبات التغطية

- **الحد الأدنى**: 70%
- **الهدف**: 80%+
- **الملفات الحساسة**: 90%+

---

## 📌 الالتزامات

### رسائل الالتزامات

استخدم الصيغة التالية:

```
<type>(<scope>): <subject>

<body>

<footer>
```

#### الأنواع

- **feat**: ميزة جديدة
- **fix**: إصلاح خطأ
- **docs**: تحديثات التوثيق
- **style**: تنسيق الكود
- **refactor**: إعادة هيكلة الكود
- **perf**: تحسينات الأداء
- **test**: إضافة أو تحديث الاختبارات
- **chore**: مهام صيانة

#### أمثلة

```
feat(tasks): add task filtering by status

Allow users to filter tasks by their current status.
This makes it easier to find specific tasks.

Closes #123
```

```
fix(auth): prevent token expiration during long tasks

The token was expiring while tasks were running.
Now we refresh the token automatically.

Fixes #456
```

---

## 🔄 Pull Requests

### قبل فتح PR

- [ ] تحديث الفرع من upstream
- [ ] تشغيل الاختبارات بنجاح
- [ ] تشغيل فاحصات الكود (black, ruff)
- [ ] إضافة اختبارات للميزات الجديدة
- [ ] تحديث التوثيق
- [ ] تحديث CHANGELOG (إن وجد)

### وصف PR

```markdown
## الوصف
وصف مختصر لما يفعله هذا PR.

## النوع
- [ ] ميزة جديدة
- [ ] إصلاح خطأ
- [ ] تحديث التوثيق

## الاختبارات
- [ ] اختبارات جديدة
- [ ] اختبارات موجودة تمر
- [ ] تغطية كافية

## الفحوصات
- [ ] لا توجد أخطاء linting
- [ ] التوثيق محدثة
- [ ] CHANGELOG محدثة

## الروابط
Closes #issue-number
```

### عملية المراجعة

1. **فحص الكود**: سيقوم المراجعون بفحص الكود
2. **الملاحظات**: قد تتلقى طلبات للتعديل
3. **التحديثات**: قم بإجراء التعديلات المطلوبة
4. **الموافقة**: بعد الموافقة، سيتم دمج PR

---

## 🎯 الأولويات

نركز على:

1. **الأمان**: أي مشاكل أمنية لها أولوية عالية جداً
2. **الأداء**: تحسينات الأداء مهمة جداً
3. **الميزات**: الميزات الجديدة المفيدة مرحب بها
4. **التوثيق**: تحسينات التوثيق مهمة

---

## 📚 موارد مفيدة

- [GitHub Help](https://help.github.com)
- [Git Documentation](https://git-scm.com/doc)
- [Python Style Guide (PEP 8)](https://pep8.org)
- [React Documentation](https://react.dev)
- [FastAPI Documentation](https://fastapi.tiangolo.com)

---

## ❓ الأسئلة؟

- فتح issue مع التصنيف `question`
- شارك في Discussions
- تواصل مع المجتمع

---

شكراً على مساهمتك! 🎉

</div>
